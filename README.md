# quickgig - a mobile app that connects job poster and short term/part time job seekers

capstone

update1:
Since my last push, I update the app wwhere is the login form is functionable; it is connected to my database; I added an home screen page that will navigate if I logged an registerd account but the home screen has static content for now, but there, you will see that there is some icons and one of them is for profile page navigation; it is the only icon that where you can navigate with for now; that profile page is still not finished, some feature is for static display only but some is working dynamically like reviews (there is so much to add but the api is working when I test it in postman) and the user's name is from the database. That is all for this updat.
